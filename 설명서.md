# 키즈카페 예약 알리미 — 사용 설명서 (클라우드 버전)

지정한 **서울형 키즈카페 예약 캘린더**를 GitHub의 서버가 대신 5분마다 확인해서,
**마감되어 있던 날짜·회차가 예약가능으로 바뀌는 순간** 텔레그램으로 알려줍니다.

이전 "하얏트 예약 알리미"와 다른 점: **내 PC나 휴대폰을 켜 둘 필요가 전혀 없습니다.**
GitHub가 무료로 제공하는 서버(GitHub Actions)가 알아서 계속 돌아갑니다.

---

## 1. 사전 준비물

- GitHub 계정 (없다면 https://github.com/signup 에서 무료로 생성)
- 이 폴더 전체: `G:\내 드라이브\ai\키즈카페예약알리미\`
- 텔레그램 봇 토큰 / Chat ID (이전에 만든 것을 그대로 씁니다 — 테스트 완료됨)
  - 봇 토큰: `8830977104:AAHPgzEbb8pH3tFkFNQ9nki2KnpPYo1uRwE`
  - Chat ID: `5528354233`

---

## 2. GitHub 저장소 만들고 코드 올리기 (최초 1회)

1. https://github.com/new 접속
2. Repository name: 예) `kidscafe-watcher` (원하는 이름 아무거나 가능)
3. **Public**(공개) 선택 — Actions 실행 시간이 완전 무료입니다.
4. "Create repository" 클릭 (README 추가 등 다른 옵션은 체크하지 않아도 됩니다)
5. 생성된 저장소 페이지에 나오는 안내대로, 이 폴더에서 아래 명령을 순서대로 실행합니다.
   (Windows 터미널/PowerShell에서 이 폴더로 이동한 뒤 실행)

```
git init
git add .
git commit -m "키즈카페 예약 알리미 최초 등록"
git branch -M main
git remote add origin https://github.com/<내깃허브아이디>/kidscafe-watcher.git
git push -u origin main
```

`<내깃허브아이디>` 와 저장소 이름은 실제 만든 것으로 바꿔주세요.

---

## 3. 텔레그램 비밀정보 등록 (최초 1회)

토큰과 Chat ID는 코드에 직접 적지 않고, GitHub의 "비밀금고(Secrets)"에 따로 저장합니다.
(이렇게 하면 저장소를 공개해도 비밀값은 다른 사람에게 보이지 않습니다.)

1. 방금 만든 저장소 페이지 → **Settings** 탭
2. 왼쪽 메뉴에서 **Secrets and variables → Actions**
3. **New repository secret** 클릭
   - Name: `TELEGRAM_BOT_TOKEN`  /  Value: `8830977104:AAHPgzEbb8pH3tFkFNQ9nki2KnpPYo1uRwE`
   - 저장 후, 같은 방법으로 하나 더 추가
   - Name: `TELEGRAM_CHAT_ID`  /  Value: `5528354233`

---

## 4. 확인

1. 저장소 페이지 → **Actions** 탭 → 왼쪽에서 "키즈카페 예약 감시" 클릭
2. **Run workflow** 버튼으로 1회 수동 실행해서 정상 작동하는지 확인
   (초록색 체크가 뜨면 성공. 클릭해서 로그를 열어보면 "최초 실행입니다…" 같은 문구가 보입니다)
3. 이후로는 **5분마다 자동으로** 실행됩니다. 아무것도 안 해도 됩니다.
4. 마감되어 있던 자리가 열리면 텔레그램으로 메시지가 옵니다.

> GitHub 공식 정책상 예약 실행은 "5분 간격"으로 설정해도 서버 상황에 따라
> 실제로는 5~15분 정도 늦게 실행될 수 있습니다. (초 단위로 딱 맞춰 도는 것은 아닙니다.)

---

## 5. 설정 바꾸기

`monitor.py` 파일 위쪽 `TARGET_URL` 을 바꾸면 다른 키즈카페/다른 지점도 감시할 수 있습니다.
바꾼 뒤에는 저장하고 아래 명령으로 다시 올리면 됩니다.

```
git add monitor.py
git commit -m "감시 대상 변경"
git push
```

확인 주기를 바꾸려면 `.github/workflows/watch.yml` 의 `cron: "*/5 * * * *"` 부분을 수정하세요.
(예: 10분마다 → `*/10 * * * *`)

---

## 6. 참고 (기술 정보 / 알아두면 좋은 점)

- 브라우저를 켜지 않고 **페이지 HTML을 그대로 읽는 방식**입니다. 이 사이트는 달력 화면 안에
  회차별 "잔여 인원" 숫자가 이미 포함되어 있어서, 날짜를 일일이 클릭할 필요가 없습니다.
- **알림 조건**: 어떤 회차의 잔여 인원이 `0(또는 정보 없음) → 1 이상`으로 바뀌는 순간입니다.
  - 취소가 생겨 자리가 하나 풀리는 경우
  - 매주 화요일 15시, 새로운 주(2주 뒤 분)가 예약 화면에 통째로 새로 열리는 경우
    도 함께 잡힙니다 (이때는 다수의 날짜가 한 번에 알림에 뜰 수 있습니다).
- 이번 달 화면만 확인합니다(사이트 특성상 예약은 항상 가까운 미래 2주 이내로만 열리므로
  실무적으로 충분합니다).
- 처음 실행할 때는 비교 대상이 없어 알림을 보내지 않고, 현재 상태만 저장합니다.
- 사이트 구조가 바뀌어 읽기에 실패하면, 알림 대신 오류 로그만 남기고 다음 5분 뒤 자동으로
  다시 시도합니다(워크플로 자체가 실패로 표시되지는 않습니다).
- 예약 자체는 자동으로 하지 않습니다. 알림을 받으면 직접 예약 페이지에서 진행하세요
  (로그인이 필요합니다).
